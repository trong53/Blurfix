{% extends 'layout.html.twig' %}

{% block title %}Favoris{% endblock %}

{% block content %}
    <div class="mt-8">
        <div class="flex justify-between">
            <h2 class="text-white text-4xl">Favoris</h2>
            <div class="flex justify-center items-center">
                
                <label for="filter_favorite" class="mr-4"><i class="fi fi-filter text-[#1190ea]"></i></label>
                <select name="filter_favorite" id="filter_favorite"
                        class="text-lg text-gray-500 bg-transparent border-0 border-b-2 border-gray-200 appearance-none dark:text-gray-400 dark:border-gray-700 focus:outline-none focus:ring-0 focus:border-gray-200"
                        onchange="window.location = '/pages/favorite.php?filter_favorite='+this.value">
                        {% for category in categories %}
                            <option value="{{ category }}">{{ category }}</option>
                        {% endfor %}
                </select>
                
            </div>
        </div>
        <div class="flex flex-col md:flex-row flex-wrap justify-around mt-8">
            {% for movie in movies %}
                {% include 'components/movie_card_favorite.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}